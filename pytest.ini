[pytest]
# --headed is NOT listed here because we manage the browser in conftest.py
# directly (headless=False).  Listing it here would tell pytest-playwright
# to launch its OWN headed browser which we don't want.
addopts = -v